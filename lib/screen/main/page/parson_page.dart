import 'package:flutter/material.dart';

class ParsonPage extends StatelessWidget {
  const ParsonPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Icon(Icons.person),
    );
  }
}
